[pytest]
DJANGO_SETTINGS_MODULE = securepass.settings
python_files = tests.py test_*.py *_tests.py
python_classes = Test*
python_functions = test_*
addopts = 
    --tb=short
    --strict-markers
    -v
filterwarnings =
    ignore::DeprecationWarning
